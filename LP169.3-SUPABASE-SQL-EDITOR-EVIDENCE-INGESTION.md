# LP169.3 — Supabase SQL Editor Evidence Ingestion

## Classification

**`SUPABASE_SQL_EDITOR_EVIDENCE_INGESTION_READY`** — tooling is complete, but the owner CSV was not available in this environment. **`OWNER_SQL_EDITOR_EXPORT_INGESTION_REQUIRED`** remains the truthful ingestion result. No evidence contents were invented.

## Windows owner workflow

1. In the authenticated Supabase Dashboard SQL Editor, run the complete, unchanged `tools/lp169/lp169-owner-metadata-query.sql`.
2. Confirm the editor returns one column named `lp169_owner_metadata` and exactly one result row.
3. Export that result to CSV without opening or resaving it in spreadsheet software.
4. From the repository root run:

```powershell
npm run ingest:lp169:sql-editor -- "C:\full\path\to\Supabase Snippet Untitled query (4).csv"
npm run audit:lp169
npm run certify:lp169
npm run verify:lp169
npm run test:lp169
npm run test:lp168
npm run test:lp167
```

The path is explicit; no Downloads or Desktop location is assumed. The dashboard workflow requires neither a database password nor a separate PostgreSQL command-line client. The existing capture script remains responsible only for safe GitHub CLI and Supabase CLI metadata and prints the SQL Editor requirement.

## Governed boundary

The query reads only approved information-schema catalogs, policy/index catalogs, Storage bucket identity, and aggregate Storage object counts. It does not retrieve object names, object content, MIME metadata, authentication rows, credentials, or user data. It contains no mutation or DDL statements.

The ingester uses a quoted-field CSV state machine, fatal UTF-8 decoding, safe BOM normalization, exact row/column cardinality, strict metadata keys and safe item fields, secret-pattern rejection, deterministic sorting, canonical JSON, and atomic replacement. A rejection reports only `SANITIZATION_FAILED`; it neither displays the rejected cell nor changes prior governed evidence.

Dashboard metadata can advance observed schemas, tables/views, columns, routines, constraints, indexes, triggers, policy inventory, Storage bucket identity, and aggregate bucket counts. Aggregate count `508`, if observed, is not by itself proof of 254 exact package names plus 254 exact certificate names. API reachability, Edge Function behavior, secret-name presence, CORS, redirects, anonymous runtime reads, write denial, exact names, hashes, and MIME types remain unavailable unless separately evidenced. No deployment, activation, production write, schema/policy change, or Storage change is authorized.
