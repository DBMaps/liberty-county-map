# LP188.12 — Wave 0 reconciliation contract audit

## Disposition

The owner execution is classified **`WAVE0_EXECUTION_PASSED_RECONCILIATION_CONTRACT_MISMATCH`**. The protected browser test and all 28 county-selection journeys passed. The returned zero failures, zero open severity-1/severity-2 defects, no production mutation, no activation, and the sanitized 28-entry Cloudflare-managed RUM blocking audit remain authoritative. This is not a Wave 0 failure, but it is not LP188.12 closure PASS.

## Assertion ownership

LP188.11F2 assigns all six assertions to protected Wave 0. LP188.11F3/F4 did not move any of them to a later dimension. They instead recorded that only artifact and operational fixtures existed, and that fixture authority was incomplete. The three apparently missing families have these dispositions:

| Assertion | Owner | Current disposition |
| --- | --- | --- |
| `CERTIFIED_ARTIFACT_STABILITY` | Wave 0 | Not completed by this journey: it observed runtime `validationState`, but did not compare governed response bytes, SHA-256, and byte length. |
| `OPERATIONAL_COUNTY_RESULT_STABILITY` | Wave 0 | Completed for the exact 28 FIPS by the passed owner county-selection journeys. |
| `COUNTY_BOUNDARY_ISOLATION` | Wave 0 | Outstanding. LP188.11F4 requires owner-approved edge coordinates and prohibited adjacent-county expectations. The earlier 215-county `boundaryValidated` dimension is package/deployment containment evidence and is explicitly not this protected edge journey. |
| `CONSUMER_RESULT_STABILITY` | Wave 0 | Outstanding. County selection is not an exact per-FIPS consumer result/output comparison. |
| `FALLBACK_BEHAVIOR_STABILITY` | Wave 0 | Outstanding. LP188.11F4 requires an approved deterministic safe trigger and exact fallback/prohibited-substitution result. |
| `ROUTE_AWARENESS_STABILITY` | Wave 0 | Outstanding. LP188.11F4 requires governed protected route/watch inputs and exact expected visible state; prior offline Talco/Goodrich evidence does not authorize protected provider execution. |

Thus options (a) and (b) do not apply to the three named missing families. Option (c) applies: Wave 0 owns them, but the current harness did not execute them. No prior evidence may be promoted into per-county PASS records.

## Contract mismatch

The browser harness filtered the 56 governed fixtures down to `OPERATIONAL_COUNTY_RESULT_STABILITY`, then hard-coded three assertion names into every result. Only the active-FIPS equality was a complete assertion check. The runtime package hook did not contain the governed SHA-256/length comparison required for certified-artifact stability, and no consumer result fixture was entered or compared. Boundary, fallback, and route-awareness had neither fixtures nor browser steps.

The old `reconcile()` then unioned assertion-name strings across county results and required all six names. This simultaneously caused the reported deterministic `FAIL` and made completion forgeable by adding strings. Reconciliation now uses the contract's explicit Wave 0 ownership and requires a `PASS`, `executed: true`, and non-empty passed evidence checks with references for every Wave-0-owned assertion. Assertion names alone cannot complete anything. Incomplete valid execution evidence is reported as `PENDING`, not as a failed browser execution.

The later LP188.11E dimensions—215-county regression, consumer observation, telemetry, rollback, operational observation, and independent review—remain explicit closure dependencies. Reconciliation does not confuse those dimensions with Wave 0 assertion families. Previously completed deployment, runtime, and package-boundary dimensions remain immutable and must not be rerun.

## Execution consequence

Do **not** rerun the existing browser command: it would repeat the same county-selection dimension and still could not prove the outstanding assertions. First govern the missing exact fixtures and expected results and extend the read-only harness with the actual checks. A later owner execution will be required only for those genuinely outstanding Wave 0 checks (and the precise artifact comparison), without duplicating the completed 28-county selection evidence. No Gridly runtime modification or Cloudflare redeployment is required by this audit.

The immediate owner command is an ingestion/reconciliation command, not a browser run:

```sh
npm run reconcile:lp18812 -- evidence/lp18812/wave0-partial-result.json
```

With the current owner artifact this must remain pending until governed executed evidence exists for every Wave-0-owned assertion.
