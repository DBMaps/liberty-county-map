# LP188.12 — remaining Wave 0 fixture and execution design

## Result

The governed design pass is complete; no owner execution was performed. Wave 0 remains `PENDING`, and `OPERATIONAL_COUNTY_RESULT_STABILITY` remains completed exclusively by the immutable owner-execution migration. The new machine-readable authority is `reports/lp18812/wave0-remaining-fixture-contract.json`.

## Exact fixture authority

| Assertion | Fixtures | Authority and expected result | Future proof |
| --- | ---: | --- | --- |
| `CERTIFIED_ARTIFACT_STABILITY` | 28 | LP188.5 identities and LP188.11F4's exact `/counties/<FIPS>.json` URL, byte length, and SHA-256 | One authenticated raw-byte GET per URL; status 200 **and both** length/hash equality. DOM/runtime `validationState` cannot pass. Evidence retains hashes and lengths, not payloads. |
| `COUNTY_BOUNDARY_ISOLATION` | 4 | Two 0.0002-degree interior-offset points on each side of the committed Liberty–Hardin and Liberty–Chambers Census shared boundaries | Protected runtime resolves exactly one expected FIPS and never the named adjacent FIPS. No point is the boundary coordinate. |
| `CONSUMER_RESULT_STABILITY` | 28 | The exact LP153/LP188.11F4 governed ZIP for each operational FIPS, evaluated on the awareness query **preview** surface | Exact `single_match`, county FIPS/name, resolved state and candidate count. It must not apply county selection, so the completed operational journey is not duplicated. |
| `FALLBACK_BEHAVIOR_STABILITY` | 1 | Repository-controlled unknown ZIP `00000` through the existing ZIP resolver/manual-setup contract | Exact unresolved `manual_fallback`, zero candidates, manual action, unchanged county; any county/nearby-ZIP substitution is prohibited. |
| `ROUTE_AWARENESS_STABILITY` | 1 | Existing LP178 Talco three-point route and Goodrich near-route/remote fixtures | Session-local Route Watch retains all three points, includes Goodrich, excludes remote, and leaves the awareness area unchanged. No provider call, official claim, hazard creation, or production mutation is required. |

Every JSON fixture records its assertion and fixture IDs, exact input, expected and prohibited results, FIPS/scope, sources, execution mechanism, browser requirement, and mandatory PASS evidence fields. This prevents assertion-name presence from becoming evidence.

## Governance-gap audit

LP188.3/LP188.5/LP188.6 supplied package identities and promotion constraints; LP188.10/LP188.11 supplied Wave 0 scope and execution controls. They did not contain protected edge, consumer-preview, fallback-trigger, or route fixture contracts. The gap is closed here from repository-controlled Census geometries, the exact governed ZIP registry/resolver semantics, and existing LP178 Talco/Goodrich fixtures. No LP154/LP155 `NOT TESTED` narrative was promoted into expected evidence, and no official/DriveTexas expectation was invented.

## Future execution ownership

Exactly **two** narrowly scoped owner executions are proposed:

1. A direct protected authenticated GET batch for the 28 artifacts, with a 120,000 ms finite budget and origin-scoped Service Auth.
2. One 390×844 protected Chromium execution for 4 boundary points, 28 resolve-only consumer previews, 1 fallback, and 1 session-local route fixture, with a 300,000 ms finite budget.

All browser mutation methods remain blocked, credentials remain memory-only and origin-scoped, and sanitized request audits are mandatory. Neither execution selects operational counties, deploys Cloudflare, changes runtime, activates counties, mutates production, or creates hazards. This document intentionally provides no owner command; execution evidence must not be created during design.

## Reconciliation

Future ingestion appends one structured `assertionOutcome` only when every fixture for that family actually executes and passes with non-empty evidence references. It must preserve the migrated operational outcome rather than overwrite it. Partial, missing, names-only, mutation-observing, activation-observing, failure, or open S1/S2 evidence remains fail-closed as `PENDING`. Wave 0 becomes `PASS` only after all six owned families contain actual executed PASS outcomes and every existing reconciliation gate remains satisfied.
