# LP219.1 — Live Awareness Surface Observation and Count Parity

## 1. LP219 contract
LP219 created a read-only, identity-deduplicated governed evidence audit. LP219.1 retains that contract and observes production models before using DOM facts as parity evidence.

## 2–4. Pecos contradiction and prior observer defect
Owner acceptance for Town of Pecos (`reeves-tx`) visibly showed **3 active issues nearby**, while LP219 reported `displayedActiveIssueCount: 0` and empty publication arrays. The zero was an observer defect: it read only a dataset selector and neither independently parsed rendered count copy nor inspected structured surface models.

## 5–6. Raw and governed evidence
The owner-provided replay has three current records: cleared community report `eb1db382-e1c7-4d88-94de-a5a1fa78e5c5`, blocked-crossing community report `64dd2efd-1bae-4b94-8026-796b420b0619`, and disabled-vehicle active hazard `095e4648-d1dc-4608-8002-2b20f4d14133`. Existing policy yields two Location Context eligible active conditions: blocked crossing and active hazard.

## 7. Cleared lifecycle classification
`cleared` is explicitly `CURRENT_HISTORY_INACTIVE_CLEARED`: it may remain a current retained lifecycle record but is not an active condition. This is an audit classification, not a report deletion or subtype policy change.

## 8–9. Location Context production path and DOM parity
Production builds `activeCount` in the portrait Location Context renderer from the shared active-issue contract, falling back to Alerts, active-awareness, summary, hazard consistency, rendered marker, and visible-alert counts. Rendering preserves that value in `data-active-awareness-count` and emits the visible `N active issue(s) nearby` meta copy. LP219.1 reads the dataset as production state and independently parses bounded Location Context text for `locationContextDomCount`; it exposes parity explicitly. Structured area-scoped report/hazard lists provide recoverable production identities. Unmatched items, unexpected IDs, and missing governed IDs remain separate.

The browser replay is still owner-required. LP219.1 does **not** assert that the cleared record caused the production count of three until the structured production IDs prove it.

## 10. Community Pulse observer
The observer reads the retained Community Pulse model first and reconciles its area-scoped report and hazard rows. The visually adjacent Travel Brief/KBYG copy is not treated as Community Pulse merely because of screen position.

## 11. Alerts observer
The observer reads `getAlertsSurfaceSnapshot().alerts` before bounded alert DOM identity attributes. An available empty array is `OBSERVED_EMPTY`, not a missing observer and not automatically a propagation failure.

## 12. KBYG observer
Community and Official Roadways remain distinct. Existing diagnostic identity attributes are observed independently, along with visibility and item counts. Empty observation and unavailable observation are distinct. Community-report ownership remains undefined.

## 13. Map observer
Map observation combines existing `unifiedIncidentLayer` and `crossingMarkers` registries only in the audit adapter. It records marker-provided evidence/report/incident identities and preserves unmatched marker items. Rendering and layer ownership are unchanged.

## 14. Popup observer
Popup observation is bounded to existing popup identity attributes. Text is not promoted into governed identity. A visible identity-less popup is reported as unmatched/identity unavailable.

## 15. Evidence identity reconciliation
Consumer IDs are compared with canonical evidence IDs and raw provider/report/incident IDs. Structured consumer objects use the same governed identity function. Identity-less consumer rows remain unmatched and never become governed evidence.

## 16. Observer status taxonomy
`OBSERVED_MATCHED`, `OBSERVED_UNMATCHED`, `OBSERVED_EMPTY`, and `NOT_OBSERVED` distinguish evidence publication from observer capability. `IDENTITY_UNAVAILABLE` replaces false propagation-failure claims when the observer is absent. Surface visibility is independent of observation availability.

## 17. Per-surface eligibility
Every evidence row exposes `surfaceEligibility.<surface> = { eligible, policyStatus, reason }`. `eligible: null` and `PRODUCT_CONTRACT_UNDEFINED` are retained for unresolved ownership; lifecycle inactivity and geography are explained without manufacturing policy.

## 18–19. Pecos diagnostic and remaining unknowns
The fixture proves **3 current raw / 2 governed eligible** and can represent production/DOM count 3 without inventing an identity. Live classification depends on replay: if all three structured production IDs are observed, the cleared ID is an unexpected production item; if only two IDs are recoverable, the remaining consumer item is explicitly unmatched. Browser visibility, exact live registry identity retention, and the source of any third production item remain owner-replay unknowns.

## 20. Cienegas Terrace replay protocol
After Pecos: select Cienegas Terrace / Val Verde County, wait for the current transition/provider generations, call `window.gridlyGovernedAwarenessAudit()`, and capture raw/current/stale/duplicate breakdowns, production and DOM counts, production IDs, unmatched items, unexpected/missing IDs, and every surface observation. Do not repair the four-count state during this replay.

## 21. Protected C/J/I foundations
Canonical geography (C), transition/generation ordering (J), and watched-crossing identity/count (I) remain closed and unchanged.

## 22–27. Deferred scope
Family A, Family K, Family L, generated-incident propagation, community-report Alerts/KBYG policy, and performance work remain deferred. No Location Context count, wording, map rendering, Alerts publication, KBYG publication, stale deletion, or consumer policy behavior is changed.

## 28. Tests
`test:lp2191` covers production and DOM counts, parity, empty/missing/unmatched states, identity reconciliation, cleared current-versus-active lifecycle, per-surface policy, undefined contracts, split map registries, duplicate/stale protection, generation ordering, Pecos 3/2, and locality-neutral instrumentation. The LP219 and protected regression suites remain required. Browser acceptance is not fabricated and remains blocked on owner production replay.
