# LP219.2A — Active-Issue Reconciliation Operand Capture

## 1–4. Contradiction and prior RCA

Owner evidence for Town of Pecos / Reeves County is **3 DOM / 3 production / 2 governed active**. The governed rows are a cleared community report (`CURRENT_HISTORY_INACTIVE_CLEARED`), an active blocked-crossing report, and an active disabled-vehicle hazard. LP219.2 traced the displayed count through `crossingsLine`, `normalizeGridlyMobileAwarenessPanelSummary()`, and `getGridlyReconciledAwarenessActiveIssueCount()` to LP214 max reconciliation. The owner timeline stayed at zero through `2026-08-22T02:30:49.531Z`, changed 0 → 3 at `2026-08-22T02:30:51.173Z`, and then recorded 3 → 3. LP219.2 reconstructed zero-valued operands after the invocation, so it could not explain that transition.

## 5. Complete production candidate inventory

The reconciliation has exactly these candidates:

| Production expression | Source | Semantics and retention |
|---|---|---|
| `safeNumber(safeSummary.sharedActiveIssueContract?.activeIssueCount)` | shared contract scalar | Upstream contract filtering/dedupe; may be retained in the summary; contributor identities are retained by that contract. |
| `safeNumber(counts.activeIssueCount)` | invocation counts scalar | Raw `hazardCount + reportCount + crossingReportCount`; inherits the builders' geography/lifecycle and may include retained report rows. |
| `safeNumber(counts.hazardCount)` | invocation counts scalar | Bottom hazard classification/distinct fallback; geography comes from summary selection; dedupe may occur in classification. |
| `safeNumber(counts.reportCount)` | invocation counts scalar | Non-crossing plus crossing report count; collection-derived and can include retained/history rows supplied in `activeReportsInArea`. |
| `safeLength(safeSummary.activeReportsInArea)` | exact summary collection | Raw length, already geographically selected upstream; no reconciliation-time lifecycle filter or dedupe; can include cleared/history rows and retained values. |
| `safeLength(safeSummary.activeHazardsInArea)` | exact summary collection | Raw length, already geographically selected upstream; no reconciliation-time lifecycle filter or dedupe; may be retained. |
| `safeNumber(safeSummary.reportCount)` | summary scalar | Upstream-assigned raw scalar; filtering/dedupe and retention are owned upstream; contributor identity is not present on the scalar. |
| `safeNumber(safeSummary.activeReportsInAreaCount)` | summary scalar | Upstream collection-count scalar; upstream geography/lifecycle; identities remain available only through the associated collection. |
| `safeNumber(visibleCountModel?.visibleAlertIncidentCount)` | visible consistency model scalar | Visible-alert projection, filtered/deduped by that model; may reflect its current model snapshot. |
| `safeNumber(visibleCountModel?.renderedMarkerCount)` | visible consistency model scalar | Rendered marker cardinality, deduped by marker/model ownership and geographically visible. |
| `safeNumber(visibleCountModel?.bottomAwarenessDisplayedHazardCount)` | visible consistency model scalar | Display-model scalar; derives from visible hazard consistency model. |
| `currentVisibleIncidentCount` | `gridlyGetCurrentCountyVisibleIncidentAudit(activeCountyId).currentVisibleReportCount` | County-matched visible audit scalar; can reflect the audit's retained state; contributor identity is not retained in this scalar. |

After reconciliation, `alertsGroupedIssueCount` is an additional final-normalization scalar only when positive. It is captured by the projection audit and can be reported as a tied or sole final winner; it is not falsely presented as an operand inside the reconciliation function.

## 6–9. Invocation capture and winner evidence

Immediately before return, production now materializes the 12 exact expression values once, applies the unchanged `Math.max`, and captures timestamp/generations, winners, returned value, scalar provenance, and bounded collection identities. The report and hazard collections are projected at that instant, with a 100-row maximum and no provider payload dump. The projection audit carries the reconciliation winner through the final Alerts max and records all tied winners.

The deterministic Pecos live-shape fixture has `safeNumber(counts.activeIssueCount) = 3` (two retained reports plus one hazard) and explicitly identifies that raw aggregate as winner. Its contributor collection projection identifies `cleared`, `blocked_crossing`, and `disabled_vehicle`; the exact report and hazard collections are also captured separately. Thus the deterministic hypothesis proves the cleared report participates in that fixture's winning aggregate, alongside the blocked-crossing report and disabled-vehicle hazard. This does **not** claim the owner browser's live winner; owner replay remains authoritative.

## 10–13. Roles and revision protection

* **Cleared report:** identifiable as `subtype: cleared` plus `cleared: true` when its exact collection participates.
* **Blocked crossing:** identifiable as `community_report / blocked_crossing`.
* **Disabled vehicle:** identifiable as `active_hazard / disabled_vehicle`, including its governed evidence ID when directly present.
* **Revision counter:** `gridlyOfficialRoadwayAwarenessRevision` is not passed to the reconciliation operand map. Generation/revision values occur only as diagnostic metadata and cannot win.

## 14–15. Async timeline and owner replay

Every normalization event now records previous/new counts, invocation operands, winning names/value, and bounded winning identities. The latest event and the first event that produced the current count have dedicated references; later 3 → 3 calls do not erase the first-producing reference. Older transition generations remain rejected by the existing monotonic guard.

Exact live RCA still requires the owner to return to Town of Pecos / Reeves County and run:

```js
window.gridlyLocationContextProductionAudit()
```

The owner should inspect `locationContextWinningOperands`, `locationContextWinningValue`, `locationContextInvocationOperands`, `locationContextWinningCollections`, `locationContextWinningItems`, `locationContextLatestEvent`, `locationContextFirstProducingEvent`, and `locationContextUpdateTimeline`.

## 16–18. Behavior, tests, and next boundary

This milestone is read-only instrumentation. It does not change Math.max semantics, lifecycle, geography, collections, counts, generated incidents, map behavior, or any consumer publication. The LP219.2A test covers exact candidates, explicit/tied winners, scalar provenance, bounded collection identities, all three Pecos fixture roles, revision separation, timeline retention, generation protection, unchanged return, and absence of locality special cases. Existing LP219–LP214 and certification suites remain the regression boundary.

The next repair boundary begins only after owner replay identifies the actual live winning operand. LP219.2A intentionally stops before filtering, deduping, lifecycle repair, or consumer behavior changes.
