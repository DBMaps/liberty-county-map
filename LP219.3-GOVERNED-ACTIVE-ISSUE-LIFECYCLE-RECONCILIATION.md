# LP219.3 — Governed Active-Issue Lifecycle Reconciliation

## Decision record

1. **LP219 governed evidence contract.** Governed evidence owns identity, currentness, active state, geography, deduplication, and surface eligibility. Retained records are evidence, not automatically active conditions.
2. **LP219.2A winning operand.** Owner replay established that `currentVisibleIncidentCount` won the Location Context projection.
3. **LP219.2B semantics.** That diagnostic is the current-county membership count of `activeHazards + activeReports`; its contract remains `MAP_REPORT_INVENTORY_COUNTY_MEMBERSHIP_NOT_ACTIVE_LIFECYCLE`.
4. **Controlled Pecos 1 → 2.** The owner observed one active disabled vehicle, then two active issues after adding an active blocked crossing. This remains authoritative live evidence.
5. **Historical three-item state.** The runtime collection held a cleared community report, active blocked crossing, and active disabled vehicle. The first is `CURRENT_HISTORY_INACTIVE_CLEARED`.
6. **Root cause.** LP214 allowed runtime inventory and presentation cardinalities to compete in `Math.max`, so retained inactive history or marker counts could win an active-condition count.
7. **Old contract.** Shared/raw summary counts, report/hazard collection lengths, visible alert, rendered marker, displayed hazard, and current-visible inventory counts were all candidate winners.
8. **New contract.** Candidate authority is restricted to the independently governed shared active-issue count and `currentActiveVisibleIncidentCount`, whose contract is `CURRENT_COUNTY_GOVERNED_ACTIVE_LIFECYCLE_COUNT`.
9. **Inventory versus active.** `currentVisibleIncidentCount` and its items remain unchanged diagnostic inventory. The new active count requires current `true`, active `true`, not cleared/history, not stale, current-county membership, stable identity, and uniqueness.
10. **Cleared-history preservation.** No collection mutation, deletion, marker cleanup, or History behavior changed. Cleared rows remain present and are listed in `inactiveHistoryIds` and `lifecycleExcludedIds`.
11. **Blocked crossing.** An active current blocked-crossing report remains countable. Alerts/KBYG ownership is still undefined.
12. **Disabled vehicle.** An active current disabled-vehicle hazard remains countable. Its downstream Alerts/KBYG propagation is not changed here.
13. **LP214 operand audit.** The shared governed count remains eligible. Raw aggregate, hazard/report scalar, collection lengths, legacy summary scalars, visible-alert projection, rendered markers, displayed-hazard projection, and runtime-visible inventory are diagnostic-only. `lifecycleOperandAudit` records every classification.
14. **Production changes.** The governed module now publishes a shared lifecycle predicate and dual inventory/active projections. Reconciliation consumes only lifecycle-qualified evidence. Browser audits flatten both counts/items and exclusion IDs.
15. **Pecos fixture.** Deterministic live-shape evidence proves three runtime-visible records, two governed-active records, Location Context authority of two, and retained cleared history.
16. **Quiet control.** Empty evidence produces zero inventory and zero active issues.
17. **Official roadway control.** A current active official DriveTexas-shaped condition remains governed and countable through governed authority.
18. **Generated incident control.** A current active generated road incident remains governed and countable where its existing contract participates.
19. **C/J/I preservation.** County authority, generation instrumentation, and identity deduplication remain closed; there is no locality exception.
20. **LP214 guard.** The expected 97/98 result is a static protected-file immutability finding for later authorized edits to `js/app.js` and `js/governed-awareness.js`, not a runtime failure. The guard is not disabled; its baseline needs a separate formal reconciliation.

## Explicit deferrals

21. Disabled-vehicle Alerts/KBYG propagation remains deferred.
22. Blocked-crossing Alerts/KBYG product policy remains undefined and deferred.
23. Family A markup remains deferred.
24. Family K remains deferred.
25. Family L remains deferred.
26. Floydada source validity remains deferred.
27. Multi-county UX remains deferred.
28. Performance evidence and optimization remain deferred.

## Acceptance and verification

29. **Owner browser acceptance.** Not claimed. After merge, replay Pecos: disabled vehicle = 1; add blocked crossing = 2; if naturally retained cleared history exists, inventory = 3 and active/Location Context = 2. Capture `window.gridlyLocationContextProductionAudit()` and `window.gridlyGovernedAwarenessAudit()`. Replay Cienegas Terrace separately after Pecos.
30. **Tests.** `test:lp2193` covers lifecycle retention/exclusion, active controls, duplicates, county transitions, official/generated/quiet controls, candidate restriction, audit fields, lineage, and absence of a locality special case. The requested historical regression suites are run unchanged; known LP214 static guard classification is recorded rather than suppressed.
