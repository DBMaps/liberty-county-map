# LP219.4 — Governed active evidence consumer propagation

## 1. Owner Pecos evidence

The owner observed Town of Pecos (`4873493`, `reeves-tx`) with one current blocked-crossing community report and one current road hazard. LP219.3 and Location Context correctly counted two; Alerts had no data and KBYG claimed no active local issues. Browser replay after this change remains owner-required.

## 2. Exact pre-patch lineage

The runtime `activeReports`/`activeHazards` inventory reached LP219.3 `buildSnapshot`, where both identities passed current, active, identity, duplicate, stale, and geography governance. Location Context consumed a reconciled scalar authority, not a publication collection, explaining correct count parity despite empty consumer `publishedIds`.

Alerts separately rebuilt candidates through `getAlertsSurfaceSnapshot`: canonical active records were transformed through localized commute intelligence, then area filtered. This independent projection was not the governed LP219.3 eligibility projection, so an already eligible active hazard could produce no `alerts` data. KBYG's visible quiet decision came from Community Pulse's Community Presence/lightweight projection rather than LP219.3's governed KBYG eligibility collection. Thus both were recognized upstream but omitted by separate narrow consumer authorities.

## 3. Alerts root cause

The failure was candidate-authority divergence before DOM rendering: `getAlertsSurfaceSnapshot` and its helpers owned alert candidates independently of governed surface policy. The delegated panel was healthy, but an empty transformed/area-filtered candidate collection yielded `gridlyAlertDataDiagnostic() === []`. LP219.4 now supplies the Alerts candidate source from the governed `alerts` projection before existing presentation, sorting, area filtering, and rendering.

## 4. KBYG root cause

KBYG's quiet/active decision used the narrower Community Presence/lightweight active-awareness count. It did not consume a production KBYG publication authority. LP219.4 makes governed `kbygCommunity` evidence outrank a zero narrow score, while leaving all existing copy/presentation logic intact.

## 5. Blocked crossing policy

`blocked_crossing` has `alerts: null` and `kbygCommunity: null` in the existing governed policy. This is an explicit `PRODUCT_CONTRACT_UNDEFINED` policy gap, not a technical propagation defect. LP219.4 does not publish it to either surface and does not invent blocked-crossing policy.

## 6. Active hazard policy

`active_hazard` already has `alerts: true` and `kbygCommunity: true`. Its loss was a technical propagation defect. LP219.4 publishes it to both governed consumer projections.

## 7. Production changes

`buildConsumerProjection` materializes per-surface eligible records and per-ID lineage from the existing lifecycle/policy snapshot. Alerts and KBYG now consume that projection. The governed audit observes KBYG's production projection and reports eligibility, publication, omission reason, canonical geography, lifecycle, Community Pulse status, and final surface membership.

## 8. Protected systems unchanged

No report persistence, Supabase synchronization, submission, crossing/provider package, DriveTexas ingestion, weather, roadway runtime, marker/map, hazard lifecycle, Location Context reconciliation, PLACE/county membership, Route Watch, notification, or directional-intelligence ownership was changed.

## 9. Deterministic matrix

The LP219.4 suite covers quiet, active hazard, blocked-crossing policy deferral, mixed Pecos, retained cleared history, stale, duplicate, and old-area controls. Mixed Pecos remains governed-active 2, while the one policy-eligible hazard reaches Alerts and KBYG and the blocked crossing carries an explicit policy omission.

## 10. Regression results

All requested LP219.4, LP219.3, LP219.2B/A, LP219.2, LP219.1, LP219, LP218.3/2/1, LP218, LP217.1, LP217, LP216, LP213, LP202.1/2, and post-manual statewide suites passed. The focused filename-glob Location Context/history invocation selected zero files; LP219.3's nine-test suite includes and passed the lifecycle/history protections. Syntax checks and diff checks passed.

## 11. Remaining deferrals

Blocked-crossing Alerts/KBYG policy and disabled-vehicle propagation remain deferred. No legacy preferred-count assertion or LP214 baseline was weakened.

## 12. Owner acceptance

Owner browser acceptance is still required. Replay Pecos and inspect `gridlyLocationContextProductionAudit()` and `gridlyGovernedAwarenessAudit()`, especially `consumerPropagationLineage`, Alerts rows/data, and non-quiet KBYG behavior for the active hazard. This document does not claim browser acceptance.
