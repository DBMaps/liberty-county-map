# LP241.7 Owner Device Availability Reconciliation

**As of:** 2026-08-27  
**Final classification:** **OWNER_ACCEPTANCE_REQUIRED**
**Evidence boundary:** no physical Android execution is claimed. `DETERMINISTIC_EMULATION_CONTRACT` means bounded mobile-condition source-contract evidence; no rendered browser emulation was executed, not an Android-device pass.

## 1. Android device requirement RCA

OA-2 became mandatory because source inspection could not prove physical touch, mobile Chrome UI occlusion, platform font rendering, or rotation repaint. That conservative boundary was incorrectly converted into a requirement that the owner personally possess Android hardware. Gridly's existing launch policy does not require physical pre-launch certification on every supported mobile platform. The appropriate contract is deterministic cross-platform compatibility, physical acceptance on available representative platforms, no known defects, and post-launch monitoring of additional combinations. With no known Android defect, unavailable hardware is not itself a launch risk or blocker.

## 2. Which OA-2 claims are hardware-specific

| Claim | Classification | Boundary |
|---|---|---|
| Mobile viewport behavior | CERTIFIABLE_WITH_BROWSER_EMULATION | Representative portrait/landscape CSS viewport and dynamic viewport contracts; real Chrome UI occlusion excluded. |
| Touch geometry | CERTIFIABLE_WITH_BROWSER_EMULATION | Touch/coarse-pointer conditions and reachable bounds; real finger accuracy and OS gesture region excluded. |
| Increased text | CERTIFIABLE_DETERMINISTICALLY + CERTIFIABLE_WITH_BROWSER_EMULATION | Zoom enabled, wrapping and scrolling under pressure; Android font rasterization excluded. |
| Browser chrome occlusion | REQUIRES_PHYSICAL_ANDROID | Only a real device proves the current browser UI and gesture-region interaction. It is OPTIONAL_POST_LAUNCH_VALIDATION. |
| Portrait/landscape | CERTIFIABLE_WITH_BROWSER_EMULATION | Both representative viewport dimensions exercise the responsive contract. |
| Rotation repaint | REQUIRES_PHYSICAL_ANDROID | A real Android compositor transition cannot be inferred from resizing. It is OPTIONAL_POST_LAUNCH_VALIDATION. |

## 3. What can be certified without Android hardware

The deterministic audit certifies zoom permission, safe-area tokens, dynamic viewport units, responsive breakpoints, reduced motion, scrollable overlays, manifest standalone mode, icons, and service-worker lifecycle wiring. The bounded Android-like source-contract scenarios are labeled **DETERMINISTIC_EMULATION_CONTRACT**: 390×844 portrait, 844×390 landscape, and 390×844 at 200% text/zoom pressure, with touch and coarse-pointer conditions. They materially cover layout selection, reflow, safe/dynamic viewport sizing, sheet scrolling, and the control reachability contract. They do not assert Android font rasterization, physical Chrome occlusion, gesture-region behavior, or compositor repaint.

## 4. Whether Android physical testing is a launch blocker

**No.** None of the remaining Android-only observations is a distinct required product claim in the current Gridly launch policy, and no known defect points to a heightened Android risk. OA-2 is therefore `DETERMINISTIC_COMPATIBILITY_PASS`; physical Android is `OPTIONAL_POST_LAUNCH_VALIDATION`, never `PHYSICAL_DEVICE_PASS`.

## 5. PWA platform requirement RCA

OA-4's journey tests generic product behavior: installability, standalone launch/scope, cold launch, reachable controls, My Area persistence, close/relaunch lifecycle, a nonblank shell, and service-worker-controlled startup. It was coupled to Android only because OA-2 previously supplied a convenient same device. Android is not intrinsic to those product claims.

## 6. Whether Windows Edge PWA can replace OA-4

**Yes.** An installed Microsoft Edge PWA on the owner's existing Windows device truthfully exercises installability, OS-installed standalone launch, cold launch, controls, My Area persistence, close/relaunch lifecycle, nonblank shell, and service-worker-controlled startup. “Background/foreground” is translated to minimize/restore or close/relaunch; close/relaunch is the required persistence gate because desktop process suspension is OS-managed and nondeterministic. OA-4 is now the physical **Windows Edge installed-PWA smoke**, still unexecuted and not relabeled PASS.

## 7. Any remaining Android-only PWA claim

Windows cannot cover Android WebAPK packaging, Android task/background eviction, Android permission prompts, or Chrome/Android OS update integration. These are not claims made by the generic Gridly PWA launch gate and remain optional post-launch observations. If Gridly later promises Android-specific packaging or lifecycle behavior, that promise requires a separately owned physical-Android gate.

## 8. Revised owner acceptance matrix

| Entry | Status | Smallest truthful owner action |
|---|---|---|
| OA-1 Windows/current Edge | OWNER_RUNTIME_PASS | None. |
| OA-2 Android compatibility | DETERMINISTIC_COMPATIBILITY_PASS | None; physical Android is optional post-launch. |
| OA-3 notched iPhone/current Safari | AVAILABILITY_CONFIRMATION_REMAINING | First confirm access through the normal acceptance process. If available, run the existing increased-text/safe-area/rotate smoke; VoiceOver remains optional. |
| OA-4 installed PWA | MANDATORY_OWNER_ACCEPTANCE_REMAINING | Install and smoke Gridly in Microsoft Edge on the owner's Windows device. |

OA-3 is not weakened or conflated with Android. WebKit and physical notch/safe-area behavior remain separate, and the record does not presume whether the owner has the required iPhone.

## 9. Optional post-launch device matrix

| Platform | Optional observation |
|---|---|
| Physical Android/current Chrome | Font rendering, real browser-chrome occlusion, OS gesture region, physical touch comfort, and rotation/compositor repaint. |
| Android installed PWA | WebAPK packaging, Android permission surfaces, task eviction/resumption, and OS integration. |
| Additional Android sizes/vendors | Layout monitoring outside the representative emulated viewports. |
| iPhone VoiceOver | Semantic spot check; deterministic semantics and Windows assistive-technology entry are already accepted. |

## 10. Files changed

This reconciliation updates this certification, the owner matrix, certification and exception reports, the deterministic/emulated responsive evidence generator, and its regression test. No production behavior changes.

## 11. Tests

Run the LP241.7 certifier, the combined LP241.7 suite, relevant PWA suites, JSON parsing, and `git diff --check`. The bounded scenarios are deterministic emulation-contract evidence, not executed browser emulation; no physical Android execution is represented.

## 12. Final LP241.7 classification

**OWNER_ACCEPTANCE_REQUIRED.** Android owner actions are zero. Remaining acceptance consists of OA-3 availability confirmation (and the existing Safari smoke if available) plus OA-4 Windows Edge installed-PWA smoke. There is no known compatibility/accessibility defect.

## 13. Exact next owner action

On the existing Windows/current Edge computer, install Gridly as an app; close it; cold-launch it from the installed-app entry; confirm the shell is nonblank and controls are reachable; open/close Alerts; change My Area; close and relaunch; confirm My Area persists. Record PASS or the first failed step. Do not obtain Android hardware. Separately, answer only whether the normally required notched iPhone is available so OA-3 can follow its unchanged process.

## 14. Merge recommendation

**MERGE.** The matrix now tests the product contract rather than owner hardware ownership, preserves fail-closed evidence labels, keeps WebKit/safe-area acceptance separate, and retains genuinely Android-only observations as optional post-launch validation. LP241.7 becomes `CERTIFIED` only after the remaining available-platform OA-3/OA-4 acceptance is resolved.
