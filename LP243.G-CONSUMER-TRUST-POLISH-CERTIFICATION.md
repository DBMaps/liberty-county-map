# LP243.G — Current consumer trust polish certification

## Bounded audit and RCA

| Item | Current authority / template | RCA and first losing stage | Repair boundary | Protected behavior |
|---|---|---|---|---|
| G1 | `getV2PreconditionsState()` supplies the current Portrait V2 Settings helper. | The helper retained the internal/legacy storage term “Home Town”; the first loss was this consumer-copy projection. Other `homeTown` occurrences are storage, tests, diagnostics, or historical documentation. | Change that reachable helper to “Home Area” only. | Home Area identity, storage, selection, qualification, rehydration, and multi-county behavior. |
| G2 | `buildSettingsSurfaceHtml()` renders four checkboxes; `saveGridlySettingsPreferences()` normalizes them and persists `gridlySettingsV1` locally. | The controls have no delivery consumer. Route Watch does not read them to deliver messages; there is no Notification API request/construction, service-worker push handler or subscription, or backend delivery subscription. The first trust loss was present-tense Settings copy beside the controls. | Keep interactive local preferences and explicitly describe delivery as coming soon. | Existing values, local persistence, Alerts, Route Watch, and all backend/runtime systems. |
| G3 | `getV2PreconditionsState()` correctly returns an empty report reason after selection, while `refreshPortraitV2ReportCtas()` enables placement. | Selection refreshed the buttons but not the already-rendered helper. The DOM refresh was the first losing stage. | Refresh that same helper from the current precondition state; show no post-selection instruction. | Hazard types, placement, geolocation, submission, lifecycle, persistence, Supabase, toast, and Location Context. |
| G4 | `buildGridlySettingsAwarenessOptionsHtml()` renders the pending candidate and explicit confirmation. | The badge was emitted after both candidate lines into a one-column grid, so it occupied its own row. The first loss was candidate markup/layout. | Reorder badge markup and use a two-column CSS grid with a shrinkable/wrapping content column. | LP242.1 pending/confirm transaction, candidate identity, county qualification, keyboard behavior, and explicit confirmation. |
| G5 | `gridlyDriveTexasProvider.normalizeRecord()` exposes provider event `startTime`/`endTime`; `gridlyLp0455OfficialFreshnessResult()` and `gridlyLp0393OfficialPopupFreshnessLine()` own freshness display. | **Classification C:** the current authoritative record model and current source fixtures expose event bounds but no trustworthy record-level provider-update field. `state.lastRefresh` is Gridly fetch freshness and is correctly not projected as a DriveTexas update. There is no losing presentation stage for an authoritative update. | No code repair. Retain the quiet, explicit “Update time unavailable” line and provider provenance. | Source records, geometry, identity, lifecycle, filtering, membership, context, and deduplication. |

## Exact consumer copy

- Settings helper: “Set Home Town and preferences to unlock route and alerts intelligence.” → “Set Home Area and preferences to unlock route and alerts intelligence.”
- Notifications: “Choose the updates that matter to you. Preferences are saved on this device.” → “Notification delivery is coming soon. Save the updates you want to receive when it becomes available.”
- Report before selection remains “Choose a hazard to enable placement actions.” After a valid selection, the helper is absent.
- DriveTexas remains “Official Source · DriveTexas” and, without authoritative record freshness, “Update time unavailable.”

## Owner acceptance

At 320 px and 390 px portrait widths: open Settings and confirm the Home Area helper terminology; expand Notifications and confirm future-delivery language while toggles retain state after reopen; under Awareness search `Aus`, select Austin, and confirm the badge sits upper-right without collision and the full-width confirmation remains; open Report, verify the prompt before selection and its removal after Flooding while both placement actions enable; open an Official Roadway popup without record freshness and confirm provenance plus the unavailable line. Confirm vertical scrolling, bottom-dock return access, touch targets, and no horizontal overflow on each modified surface.
