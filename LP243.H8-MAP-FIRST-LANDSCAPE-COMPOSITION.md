# LP243.H8 — Map-first short-landscape composition

## Owner-selected design

LP243.H8 applies only to `(orientation: landscape) and (max-height: 500px)`. It keeps the current Gridly application and Leaflet map mounted, compacts the current top chrome and five-filter strip, and makes a collapsed bottom command disclosure the default. Portrait and the 1440×900 tall-wide layout remain outside H8.

## Component reuse audit

| Capability | Existing DOM owner / lifecycle | H8 composition |
| --- | --- | --- |
| Location Context | `.mobile-destination-command` (`#mobileDestinationCommandPanel`) | Same article; CSS visibility and `inert` only, no move or clone. |
| Search | `#mobileDestinationCommandBtn` → `openGridlyDestinationSearchSurface` / `#gridlySearchShell` | Same button and foreground lifecycle. |
| Report | `[data-v2-sheet="report"]` | Same Portrait V2 dock button and sheet lifecycle. |
| Alerts | `#gridlyAlertsDockButton` / `[data-v2-sheet="alerts"]` | Same Portrait V2 dock button and sheet lifecycle. |
| History | `#gridlyHistoryDockButton` / `[data-v2-sheet="history"]` | Same current behavior. |
| Settings | `[data-v2-sheet="settings"]` | Same Portrait V2 dock button and sheet lifecycle. |
| KBYG | `#gridlyBriefFoundationHandle` and `#gridlyBriefInteractionPanel` | Same disclosure and brief authority, compactly positioned. |
| Filters | `.gridly-v2-segments` and its five current buttons | Same single filter strip. |
| Map controls | `.gridly-v2-control-rail` | Same layer, location, and zoom controls. |
| Map | `#map` and the existing Leaflet instance | Never moved, cloned, scaled, or recreated. |

The audit proved all owners can be recomposed safely with CSS plus one presentation-local disclosure state. No event-bound feature DOM is relocated.

## State contracts

Collapsed is the default each time the short-landscape media query is entered. Gridly identity, current Community Pulse status, KBYG, all five filters, the mounted map, map controls, and the small handle remain visible. Location Context/Search and the four-action dock are visually hidden, `aria-hidden`, inert, and noninteractive.

Expanded state raises the existing Location Context/Search owner above the existing one-row Report, Alerts, History, and Settings dock. The height is bounded to the two compact rows; map padding changes so useful map area remains above. The accessible button controls both existing regions, reports `aria-expanded`, changes between explicit Show/Hide labels, and uses decorative up/down chevrons. State is memory-only for the current short-landscape session and resets on entry; it never reaches storage or product settings.

## Foreground behavior

Search continues through `openGridlyDestinationSearchSurface`. Report, Alerts, History, and Settings continue through the Portrait V2 sheet handlers. Existing H5 foreground authority remains in place, while H8 additionally suppresses the bottom region during a foreground workspace. Closing the existing surface reveals the map and retains expanded state while the media query remains active.

## Portrait isolation

All production geometry and visibility changes are inside the short-landscape media query. The handle defaults to `display: none` outside it. On leaving the query, H8 removes `inert`, `aria-hidden`, and its expanded body class, restoring the accepted Portrait DOM and geometry at 320×700, 390×844, and 430×932.

## H1–H7 reconciliation

- **KEEP:** H1 real-application ownership; H4 retired-control suppression; H5 foreground workspace authority.
- **STILL REQUIRED OUTSIDE H8:** H2 centered tall-wide shell and H3 landscape dock relationship.
- **SUPERSEDED INSIDE H8:** H5 tall-phone-like short geometry, H6 top/bottom reserves, and H7 short-landscape fixed-surface x geometry. The final H8 block follows H7 and explicitly owns short-landscape reserves, chrome, controls, map padding, command rows, and safe gutters.
- **TEST-ONLY/HISTORICAL:** H5–H7 tests remain as milestone protection, with only asset identity and H7 block slicing updated so H8's intentional final authority is not mistaken for H7.

## Remaining tall-desktop boundary

At 1440×900 H8 does not match. H2/tall-wide authority is retained pending a separate owner decision; H8 is not a general desktop redesign.
