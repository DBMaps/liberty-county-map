# Responder V1 Phase 18 — Production-Shaped Clone Rehearsal

Recorded: 2026-09-15

Source: Phase 17 commit `546590737be954684ef6826448596aba78fe595f`

Decision: **REHEARSAL PASS**

Production deployment authorization: **NOT AUTHORIZED**

## Outcome

The frozen Phase 17 migration was generated by Supabase CLI 2.117.0, copied byte-for-byte into `20260915190450_prepare_responder_production_schema.sql`, and rehearsed twice on a marked, loopback-only PostgreSQL 17.6/PostGIS 3.3.7 container. Preflight, apply, postflight, rollback, rollback refusal, deterministic reapply, migration history, security vectors, and the SECURITY DEFINER audit passed.

No production connection, read, write, configuration change, deployment, push, or merge occurred. Repository-local Supabase metadata still identifies the production project, so every CLI database operation used an explicit localhost `--db-url`; no implicit linked command was run.

## Exact environment

- Docker Desktop 4.91.0, Engine 29.8.0, linux/amd64 via WSL2.
- Base image `postgres:17.6-bookworm`, digest `sha256:f3bd19c606e442c3d7bdfa8002e03fe260a1023351e0ea4598032022b68dd6e3`.
- Rehearsal image `gridly/postgres17.6-postgis3.3.7-exact:phase18`, image ID `sha256:c6bb935d4a002250fa5207499c0a0429bb0d367d6662b67977a65d714ac4d05d`.
- PostgreSQL `17.6 (Debian 17.6-2.pgdg12+1)`.
- PostGIS `3.3.7`, built against PostgreSQL 17.6 with core geometry support.
- Target container `gridly-phase18-exact-db`, labeled `com.gridly.phase18.nonproduction=true`, bound only to a random loopback port.
- In-database marker `GRIDLY_PHASE18_NON_PRODUCTION` with production access false.

The previously supplied unmarked container was never bootstrapped. It was stopped and preserved as `gridly-phase18-exact-db-unmarked-quarantine`; the marked replacement received the exact target name.

## Clone fidelity

The clone is a synthetic production-shaped catalog and baseline reconstructed from committed Phase 17 production evidence, not a dump or live copy. This avoided all production access while reproducing the required contract:

- exact PostgreSQL/PostGIS versions and extension placement;
- `postgres`, `anon`, `authenticated`, and `service_role` BYPASSRLS posture;
- managed Auth table, enum, key, foreign-key, and helper shapes;
- 254 valid EPSG:4326 MultiPolygon county rows;
- community/history/retention baseline objects and disabled reporting;
- the 14 tracked pre-responder migration-history entries through `20260908200554`;
- no responder schema or stale responder migration marker before preflight.

`phase18_clone_fidelity.sql` returned `PHASE18_CLONE_FIDELITY_PASS`. Fidelity is sufficient for the frozen SQL and authorization contract. It does not claim production data parity, optional PostGIS raster/topology/tiger parity, or gateway parity.

## CLI generation and hash chain

Supabase CLI 2.117.0 was installed as an exact pinned dev dependency with lockfile changes. `supabase migration new prepare_responder_production_schema` generated:

`supabase/migrations/20260915190450_prepare_responder_production_schema.sql`

The generated file SHA-256 is `20fd8161586beee09c57084c4f5299aa59c1c901fdcd5890e1cb5949a83ec687`, exactly equal to `db/responder-local/phase17_production_migration.sql`. The CLI dry run reported exactly that one pending migration.

## Migration results

The read-only preflight returned `PHASE17_PRODUCTION_PREFLIGHT_PASS`. The first CLI push applied exactly one migration. Independent read-only postflight returned `PHASE17_PRODUCTION_POSTFLIGHT_PASS` with 13 private tables, 1 public projection, 11 private functions, 3 public invoker wrappers, 14 policies, 17 explicit indexes, 6 append-only triggers, zero activation, 254 counties, disabled reporting, and unchanged bounded baselines.

The empty-data rollback removed only the two responder schemas and preserved protected baselines. Independent exact-version tests proved refusal both when an external object depended on the public schema and when responder evidence existed. After local-only removal of the rolled-back migration-history row, preflight again passed, the CLI again reported exactly one pending migration, and the same file reapplied successfully.

Full catalog fingerprints before rollback and after reapply were identical:

`aaafeb1f5c39522f30f832bee3bd6d7537c8fcb53b82ccf5a28d08dfe81cc8a0`

Final CLI history matched all 15 local migrations and contained the generated timestamp/name exactly once.

## Security results

Exact-version execution passed 38 of 38 behavioral security vectors, 22 of 22 security database checks, 13 of 13 dirty fail-closed cases, and 20 of 20 migration database checks.

The SECURITY DEFINER audit found exactly 10 private definers. All 10 are owned by `postgres`, all 10 have an empty search path, and none is executable by `PUBLIC` or `service_role`. The frozen allowlist exposes six to `authenticated` and one policy dependency to `anon`. All three public command wrappers are SECURITY INVOKER; none is SECURITY DEFINER.

## Data API result

The database-level Data API contract passed: schema usage, table/function grants, RLS projection visibility, anonymous wrapper denial, authenticated wrapper invocation, and private-schema non-exposure were exercised with database roles. The tracked configuration still exposes only `public` and `graphql_public`; neither responder schema was added.

HTTP behavior was then rehearsed with the current official self-hosted PostgREST 14.17 image (digest `sha256:c9dc201e555f5d8e37e7f39cdd4df0229774996e213bfd7de8d10ac609030f2c`) in a marked, loopback-only disposable container. Anonymous projection access returned `200` with an empty array; anonymous command-wrapper invocation was denied with `401`; and an authenticated invalid request reached the SECURITY INVOKER wrapper and returned `200` with `status=invalid_request`. The container was verified and removed after the run.

This is a local Data API **PASS** for the frozen database contract. It does not configure the repository or production project to expose `responder_public`. A separately approved exposed-schema change and production-matched staging confirmation remain required before pilot use. `agency_private` must never be exposed.

## Advisor classification

Supabase CLI Security and Performance Advisors ran against the reapplied clone. There were zero errors and zero warnings. Security produced one informational finding on the synthetic baseline county table (RLS enabled without a policy; the table has no public role grants). Performance produced 25 informational findings: 10 unindexed foreign-key observations and 15 unused-index observations. Unused-index results are expected on a newly installed empty schema; all informational findings remain inputs to production traffic review rather than reasons to alter the frozen migration in this phase.

## Remaining production-only gates

- Fresh owner-controlled read-only production preflight.
- Fresh verified backup/PITR checkpoint, recovery operator, retention window, and restore authority.
- Explicit owner approval of the exact Phase 18 commit, migration timestamp, SHA, project, operator, and deployment window.
- Separately approved `responder_public` Data API exposure and production-matched staging confirmation before pilot use.
- Production Security and Performance Advisor review immediately after postflight.

The exact next action is owner review of this committed rehearsal package. No production command should run until the owner schedules a later controlled phase for the fresh recovery checkpoint and read-only production preflight.
