# V856.8 Before/After Screenshot Notes

Automated screenshot capture was attempted in this container after starting a local static server, but no browser binary or Playwright installation was available. Installing Playwright was blocked by the environment's npm registry policy (`403 Forbidden`).

Manual validation path:

1. Start `python3 -m http.server 4173` from the repository root.
2. Open `http://127.0.0.1:4173/index.html`.
3. Capture a 390 × 844 portrait screenshot before applying V856.8.
4. Capture the same viewport after applying V856.8.
5. Compare Awareness Brief hierarchy, inactive/selected map filters, location context weight, and floating Leaflet controls.

Expected after state:

- Awareness Brief appears calmer and more dominant with lighter supporting text.
- Supporting cards use softer borders and reduced shadows.
- Inactive filters recede while selected filters remain obvious.
- Location context and map controls visually support the map instead of competing with the brief.
