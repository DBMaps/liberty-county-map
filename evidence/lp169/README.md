# LP169 owner evidence drop

Place only the redacted `owner-evidence.json` produced by the procedure in the LP169 certification document here. Never place raw CLI output, environment files, tokens, URLs containing credentials, or secret values in this directory. The ingester rejects value-bearing fields, duplicate identities, unknown statuses, and recognizable credential material.
