# LP202.2 statewide consumer crossing selector RCA

## Scope and determination

This is an observational RCA. It does **not** change selector, renderer, classification, awareness-selection, package, or synchronization semantics. The authoritative Sherman capture remains `240` governed/runtime records, `46` rendered markers, and `0` consumer-selected records.

The first bad stage is `gridlyCrossingOwnedByAwarenessArea`. For a generated Sherman PLACE area, `gridlyBuildRegistryCommunityAwarenessArea` supplies neither coordinates nor geometry because Grayson is absent from the 28-county legacy `GRIDLY_COUNTY_AWARENESS_BOUNDS_BY_ID` table and the Sherman `consumerAwarenessAreas` record has no `focus`. Meanwhile `inferCrossingCity` reads lowercase aliases but not governed `CITYNAME`, so every normalized Grayson row has `city: "Unassigned"`, not `Sherman`. `isGridlyRecordInAwarenessArea` therefore fails both its text branch and its radius branch. This is not a classification failure: all 240 records are `PUBLIC_ROADWAY`.

Root-cause classifications: **`LEGACY_AWARENESS_AREA_FILTER`**, **`LEGACY_LOCALITY_OWNERSHIP_FILTER`**, **`MISSING_RUNTIME_METADATA`**, and **`STALE_REVIEW_PACKAGE_ASSUMPTION`**.

## Intended “crossings watched” contract

LP037.1 and LP037.2 establish the phrase as the number of unique, active-county runtime crossings which satisfy production visibility/classification, reportability, and selected awareness-area geographic ownership. It is not the package total, current viewport marker count, incident count, or Route Watch count (except that the existing bottom-panel wrapper deliberately preserves its separate Route Watch override). `getGridlyBottomPanelAwarenessCrossingCount` delegates to the shared selector and uses the summary only as its established non-countywide fallback.

Accordingly, selector and renderer intentionally answer different questions. The selector is awareness-area scoped. The renderer is render-policy and viewport scoped. The defect is not that these sets differ; it is that the selector's current proof of PLACE ownership depends on legacy inline metadata which the governed statewide records and generated PLACE area do not jointly provide.

## Actual production selector trace

`gridlySelectConsumerVisibleCrossings` performs these conditions in this exact order:

1. `gridlyGetActiveCountyCrossingInventory` input and `gridlyCrossingSampleMatchesCounty`;
2. `gridlyCrossingSatisfiesConsumerVisibilityPolicy`, which calls `shouldShowCrossingInLaunchMode` and enforces `PUBLIC_ROADWAY` when classified;
3. `isGridlyReportableCrossing`;
4. `gridlyCrossingOwnedByAwarenessArea`, which delegates non-countywide areas to `isGridlyRecordInAwarenessArea`;
5. FRA ID extraction and case-insensitive de-duplication.

There is no separate locality filter after awareness ownership, no separate containment gate after the awareness helper, and no later hidden eligibility gate. Text/locality and radius geometry are alternative branches *inside* awareness ownership. The audit exposes them as conservation labels without pretending they are additional production filters.

## Sherman / Grayson failing control

Selected context: Sherman; active county: `grayson-tx`; runtime/package count: 240; captured rendered count: 46.

| Stage | Kept | Rejected at stage | Exact reason |
|---|---:|---:|---|
| runtime input | 240 | 0 | Governed active inventory is synchronized. |
| active-county ownership | 240 | 0 | Normalization stamps `countyId: grayson-tx`. |
| launch/classification | 240 | 0 | Provider source is `crossing_provider_production`; all are `PUBLIC_ROADWAY`. |
| reportability | 240 | 0 | Current lowercase-field eligibility probes find no rejection evidence in the uppercase governed properties. |
| awareness ownership | **0** | **240** | `city` is `Unassigned`; selected label is `Sherman`; area `lat/lng` are null, so distance is not finite. |
| locality membership (inside prior helper) | 0 | 240 | `gridlyRecordTextMatchesAwarenessArea`: `unassigned !== sherman`. |
| radius geometry (inside prior helper) | 0 | 240 | `getDistanceMiles(null, null, ...)` cannot establish membership. No PLACE polygon is consulted. |
| FRA identity/deduplication | 0 | 0 | No row reaches it. |
| hidden legacy eligibility | 0 | 0 | None exists after dedupe. |
| final selector | **0** | — | Deterministic consequence of awareness ownership. |

Classification distribution: 240 `PUBLIC_ROADWAY`; 0 `PRIVATE_ROAD`; 0 `INDUSTRIAL`; 0 `RAIL_YARD`; 0 `TEMPORARY_ACCESS`; 0 unknown/unclassified.

Representative rejected governed rows are FRA `763684G`, `763685N`, and `763686V`. Their payloads carry `CITYNAME: SHERMAN`, coordinates, and `gridlyClassification: PUBLIC_ROADWAY`. Runtime normalization retains coordinates and classification in `props` but derives top-level `city: Unassigned`, because it does not read uppercase `CITYNAME`. Thus the information exists in the governed payload but is invisible to the selector's locality condition.

## Renderer comparison

`getGridlyPolicyVisibleCrossings` applies reportability plus `PUBLIC_ROADWAY`, verifies active-county/inventory identity, and then applies Leaflet viewport bounds and render-mode candidate rules. Its phase called “awareness-area filtering” is only active-county ownership plus active-inventory ID membership; it does not call `gridlyCrossingOwnedByAwarenessArea` or require PLACE locality metadata. The captured Sherman viewport contains 46 eligible points, so 46 render even though the consumer selector's independent PLACE-ownership proof rejects 240/240.

Renderer membership is viewport-state dependent. This repository audit certifies the captured Sherman `46`; it does not fabricate Dallas, Liberty, or additional-control marker counts without equivalent live viewport captures. Their renderer pre-viewport eligible counts are Dallas 789, Liberty 80, El Paso 198, Bexar 380, McLennan 169, and Smith 123.

## Dallas and Liberty controls

Dallas (`dallas-tx`, selected Dallas) is positive but not fully equivalent: the explicit governed Dallas focus (`32.7767,-96.7970`) lets the legacy seven-mile radius branch retain 263 of 789. Trace: `789 → 789 county → 789 classification → 789 reportable → 263 awareness/locality-geometry → 263 deduped → 263 final`. This is healthy only as a radius approximation; it does not demonstrate PLACE polygon membership.

Liberty (`liberty-tx`, selected Liberty) retains the original-era assumptions. Classification is 80 public, 28 private, 5 industrial, 1 rail yard, and 1 temporary access. Trace: `115 → 115 county → 80 classification → 80 reportable → 25 awareness/locality-geometry → 25 deduped → 25 final`. Its explicit governed/legacy focus allows the radius branch to work.

## Additional geographically distinct controls

* West Texas — El Paso / El Paso: `198 → 198 → 198 → 198 → 0 → 0 → 0`; no explicit focus and no legacy county awareness bounds.
* South Texas — Bexar / San Antonio: `380 → 380 → 380 → 380 → 234 → 234 → 234`; an explicit governed focus makes the radius approximation work.
* Central Texas — McLennan / Waco: `169 → 169 → 169 → 169 → 0 → 0 → 0`; no explicit focus and no legacy county awareness bounds.
* East Texas — Smith / Tyler: `123 → 123 → 123 → 123 → 0 → 0 → 0`; no explicit focus and no legacy county awareness bounds.

The pattern is a statewide package/context cohort defect, not Grayson-specific and not a multi-county-PLACE-only issue.

## Statewide impact

There are 202 `ACTIVE_POSITIVE` production counties. The legacy awareness-bounds table contains 28 counties, 27 of which are active-positive. Therefore **175 active-positive counties lack that fallback** and are vulnerable to the exact Sherman zero whenever the selected PLACE also lacks an explicit focus (as Sherman, El Paso, Waco, and Tyler do). This is a conservative, reproducible cohort count, not a claim that every PLACE in every one of those counties is zero: an individually supplied `focus` (Dallas and San Antonio) can mask the defect through the radius branch.

## Smallest generic repair for owner review

Do not weaken classification or make the banner equal the viewport marker count. The smallest generic repair is to make the existing awareness-ownership owner consume current governed PLACE context: preserve `CITYNAME`/PLACE identity during normalization and/or use the governed PLACE geometry/identity resolver for `placeGeoid` containment. A coordinate-radius fallback may remain a fallback, but absence of a legacy 28-county bounds entry must not be the deciding statewide eligibility condition. Confirm the preferred identity-vs-polygon contract before implementation.

Protected systems remain untouched: Wave 3A.3, governed packages/FRA authority, LP202.1/LP202.2 synchronization, renderer and classification policy, awareness selection, Shared Reports, Route Watch, Hazard Lifecycle, Alert Generation, and Supabase.

## Evidence artifacts

`tools/lp202/consumer-selector-rca-audit.mjs` reproduces the production conditions against governed packages and emits `reports/lp2022/consumer-selector-rca-audit.json`. `tests/lp2022-consumer-selector-rca.test.mjs` locks the Sherman conservation trace, Dallas/Liberty inventory controls, Liberty classification distribution, observational-only status, and the 175/202 cohort count. No consumer semantics or UI files changed.

---

## statewideCrossingAwarenessOwnershipRepair

### Repair and RCA reference

This repair follows RCA `8b68112` (repository-equivalent audit commit `10fbf22`). Governed uppercase `CITYNAME` is now part of crossing locality normalization without replacing an existing normalized `city`; casing follows the existing title-style runtime convention. The production awareness owner now evaluates non-countywide areas in this order: attached/registered governed PLACE geometry containment, governed canonical locality identity, radius only with finite explicit/governed focus coordinates, then fail closed. Countywide ownership, classification/reportability, rendering, hydration, synchronization, and source resolution are unchanged.

Before, locality omitted `CITYNAME`, then the helper tried text and an implicit seven-mile radius even when no finite center existed. After, a usable governed polygon is decisive (including an outside result), locality is used only when polygon evaluation is unavailable, and the radius cannot run without valid focus coordinates. The legacy 28-county bounds table is therefore not required for normal statewide crossing ownership.

### Controls (before → after)

* **Sherman / Grayson:** `240 input → 240 county → 240 PUBLIC_ROADWAY → 240 reportable → 0 awareness → 0 final` before; after normalization/identity ownership it is `240 → 240 → 240 → 240 → 83 awareness → 83 final`. The generic `CITYNAME: SHERMAN` path supplies the result without Grayson legacy bounds.
* **Dallas / Dallas:** legacy radius result `263`; governed canonical Dallas selection is **392**. This deliberately does not preserve the approximation. When the governed awareness-area resolver supplies polygon geometry, containment precedes this locality result.
* **Liberty / Liberty:** `115 input → 80 PUBLIC_ROADWAY → 25 legacy radius`; governed canonical Liberty locality selection is **16**. The other 35-crossing county control in the statewide audit is Cleveland and is not substituted for the requested Liberty context.
* **West Texas / El Paso:** **149** governed El Paso crossings.
* **South Texas / San Antonio:** **331** governed San Antonio crossings.
* **Central Texas / Waco:** **59** governed Waco crossings.
* **East Texas / Tyler:** **76** governed Tyler crossings.

### Statewide impact and unresolved cases

The deterministic audit evaluated one canonical governed PLACE context in every **202 ACTIVE_POSITIVE county**. **194** counties have a positive exact governed-locality result and **8** have zero; the old missing-bounds vulnerable cohort was **175**. There were **0 selector execution failures**. None of the eight zero cases is asserted to be a legitimate no-crossing geography: committed runtime polygon bytes are unavailable to this audit, so those cases remain unresolved rather than being guessed. Likewise, geometry availability is reported unresolved for 202 audit contexts; the production contract consumes geometry already attached by the governed awareness-area resolver (`geometry`, `boundaryGeometry`, or `governedGeometry`), and otherwise follows canonical locality/focus/fail-closed precedence. This audit does not manufacture or activate a second geography artifact.

The statewide audit deliberately chooses the canonical community with the greatest exact eligible `CITYNAME` membership per county so that it measures an evaluable context rather than asserting that every governed town must contain a crossing. Full evidence is preserved in `statewide-crossing-awareness-ownership-repair.json`.

### Test evidence and protected behavior

Focused tests cover uppercase locality normalization and precedence, the Sherman no-bounds path, authoritative polygon containment (including geometry overriding conflicting text), locality and valid-focus-only radius fallbacks, unchanged countywide branch, unchanged `PUBLIC_ROADWAY`/hidden classification policy, all requested geographic controls, and absence of Dallas/Sherman/Liberty production branches. LP202.1 source resolution, LP202.2 hydration/synchronization, and LP196 multi-county identity suites remain regression gates.
