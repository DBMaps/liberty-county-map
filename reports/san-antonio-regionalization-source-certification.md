# San Antonio regionalization source certification

## Status

**OFFICIAL_SOURCE_DISCOVERY_BLOCKED_NETWORK_OWNER_ACQUISITION_REQUIRED**

The execution environment could not resolve `www.arcgis.com`; no exact item, service, layer, registry, count, CRS, geometry type, or source hash was manufactured. The guarded tool queries only the City of San Antonio ArcGIS organization, proves item organization and owner identity, requires one unambiguous layer for each registry, preserves exact GeoJSON response bytes, and emits a deterministic hash manifest.

The expected 17 Community Areas and 13 Regional Centers are reconciliation inputs, not forced results. Any difference fails closed. Acquisition does not authorize manufacturing or runtime use.

## Owner sequence

Run discovery, inspect its candidates, acquire only when each source resolves uniquely, and verify the retained bytes. No command writes production runtime paths.
