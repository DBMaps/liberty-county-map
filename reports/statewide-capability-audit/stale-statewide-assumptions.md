# Stale pre-statewide assumptions

| Assumption/evidence | Classification | Current effect | Required disposition |
|---|---|---|---|
| `GRIDLY_COUNTY_AWARENESS_BOUNDS_BY_ID` has 28 entries while the runtime county registry has 254 | **STALE CONFIGURATION / STALE VALIDATION GUARD / ACTIVE RUNTIME LIMIT** | Blocks hazard/community report coordinate resolution, containment and county framing in 226 counties | P0 repository repair from governed 254 geometry |
| Crossing production and runtime cohort remains 28 | **ACTIVE PRODUCT LIMIT** | 226 counties have no package/runtime; filters are empty. Tyler is active but empty (0 certified crossings). | Manufacture/certify/promote under governance; zero-record counties explicitly NOT_APPLICABLE |
| Road runtime manifest contains 28 counties | **ACTIVE PRODUCT LIMIT** | No governed local road context in 226; basemap is not owned road data | Owner TIGER source inventory, candidate extraction, then governed integration only where product requires it |
| `runtimeSourceAvailability` says boundary available for all 254 but county configs point at the rollback 28-county package path | **STALE CONFIGURATION / STALE UI CLAIM** | Capability claim outruns the bounds/loader candidate surface | Reconcile path and derived bounds to statewide package |
| ZIP v2 is based on 28 supported counties, is partial, and says `mergeReadyForUiIntegration=false` | **ACTIVE PRODUCT LIMIT / BLOCKED GOVERNANCE** | Search is statewide but automatic ZIP-to-PLACE resolution is not | Govern statewide ZIP/community mappings; do not infer them |
| Historical LP186 documents say 28 operational and LP190.3 describes 243/11 | **HARMLESS HISTORICAL TEST/DOCUMENT when treated as immutable evidence** | Misleading if used as current truth; current app registry is 254 operational | Keep historical artifacts immutable; current audit/runtime registry controls current claims |
| Legacy Liberty/Montgomery/San Jacinto special cases remain | **ACTIVE RUNTIME LIMIT where used as submission override; otherwise historical compatibility** | San Jacinto has a special submission override; Liberty fallback remains default for invalid county normalization | Remove only after statewide deterministic regression proof; invalid identities must remain fail-closed rather than silently become Liberty |
| `gridlyNormalizeCountyId()` defaults unknown/non-operational identities to Liberty | **STALE VALIDATION RISK** | Can conceal bad caller data, though all known counties are now operational | Audit callers; preserve explicit unknown rejection at submission/containment boundaries |
| Repository tests asserting “243/11 runtime unchanged” | **HARMLESS HISTORICAL TEST** | No current runtime effect | Do not use for current counts |
| County-view crossing marker suppression and zoom/filter gates | **ACTIVE PRODUCT POLICY, not missing source** | A loaded county may show zero consumer-visible markers in a particular view | Certify intended visibility semantics separately from package availability |
| Current `verify:county-geometry-deterministic` expects legacy feature FIPS fields and `audit:county-geometry-membership` receives an undefined legacy path against the current statewide package | **STALE VALIDATION TOOLING** | Both commands fail before validating current package (`anderson-tx` FIPS identity / undefined path) | Repair verifier inputs/schema in Wave 1; do not rebuild geometry to silence it |
