import fs from 'node:fs';
const doc='tools/lp24449b/docs.mjs';let source=fs.readFileSync(doc,'utf8');
const old='The original ten stale publication observations remain in original-residual-failures.json. Current raw samples precede summary/model access; final projection, visible copy, refresh and original context transitions are recorded separately. Passing current replays prove present convergence; they do not reconstruct the historical writer order. No speculative Pulse state-management patch was made.';
const next='The original ten observations remain in original-residual-failures.json. Exact isolated/timed replays converge, but the statewide active/clear gate did not pass: three independent 254-county runs each exposed one stale publication. Conroe, Bowie and Alamo Heights retain count 1 while visible copy, grouped incidents, governed consumer rows and markers clear. Bowie remains at 1 through 2000 ms; the Alamo Heights writer trace also finds count 1 in freshly built active-awareness and shared-summary models. These findings prohibit timing-artifact reclassification and block the commit. The exact retained candidate origin is not established, so no speculative cache invalidation or count override was applied. See pulse-blocker-analysis.json for full evidence and the five passing isolated diagnostics. The earlier progress message claiming 254/254 was incorrect; the primary run passed 253/254.';
if(source.split(old).length!==2)throw Error('Pulse wording seam missing');source=source.replace(old,next);fs.writeFileSync(doc,source);
const report='tools/lp24449b/report.mjs';source=fs.readFileSync(report,'utf8').replace("gate('Original 2020 residual assertions accounted for'","gate('All 2020 original residual closure assertions pass'").replace("gate('Exact statewide inclusion'","gate('Exact statewide inclusion and local acceptance'");fs.writeFileSync(report,source);
