import {createRequire} from 'node:module';import {root,out,write} from './inventory.mjs';
const require=createRequire(import.meta.url),session=await require('./session.cjs')(),records=[];
try{
 const {page:p,evidence}=await session.newPage();await p.addScriptTag({path:`${root}/tools/lp24449b1/runtime-probe.js`});
 await p.evaluate(async()=>{const g=getGridlyManualAwarenessAreaOptions().find(g=>g.countyId==='liberty-tx'),o=g.communities.find(o=>o.label==='Dayton');if(!gridlySaveCanonicalMultiCountyPlaceHome(o.canonicalResolution,'lp24449b1-lifecycle',g.countyId))throw Error('Home save failed');await lp24449b1.settle();});
 await p.evaluate(()=>{window.b1Clear={
  async settle(){await lp24449b1.settle();},
  async home(){if(gridlyGetCurrentAwarenessContext().type!=='HOME')document.getElementById('gridlyTemporaryContextReturnHome').click();await this.settle();},
  async search(name='Liberty'){const results=await gridlySearchAddress(`${name}, Liberty County, TX`);const selected=results.find(r=>r.placeGeoid);if(!selected)throw Error('Search missing');selectGridlySearchResult(selected);await this.settle();},
  async around(){gridlyActivateForegroundAwarenessContext({timestamp:Date.now(),coords:{latitude:30.04725,longitude:-94.88737,accuracy:10}});await this.settle();},
  async reset(){gridlyLocalTestReports.clear();await this.home();map.setView([30.04725,-94.88737],14,{animate:false});},
  async add(types){const anchor=getGridlyAwarenessAnchor(),rail=crossings.filter(c=>isGridlyPublicRoadwayCrossing(c)&&isGridlyReportableCrossing(c)).sort((a,b)=>getDistanceMiles(anchor.lat,anchor.lng,a.lat,a.lng)-getDistanceMiles(anchor.lat,anchor.lng,b.lat,b.lng));let ri=0;const fixtures=types.map((type,i)=>gridlyLocalTestReports.add(type,/crossing/.test(type)?{crossingId:rail[ri++].id}:{lat:anchor.lat+(i===1?.00325:i===2?-.00375:0),lng:anchor.lng+(i===1?-.0016:i===2?.0058:0),ageMinutes:i*5}));await this.settle();return fixtures;},
  async publish(){gridlyOfficialProviderConsumerRefresh({providerId:'drivetexas',reason:'interval_live_refresh'});await this.settle();},
  capture(label,expected){const published=gridlyCommunityPulseAuditState.activeAwareness?.activeAwarenessCount,projection=gridlyGetGovernedConsumerProjection(),fresh=buildGridlyCommunityPulseModel(),counts=Object.fromEntries(['locationContext','communityPulse','kbygCommunity','alerts'].map(k=>[k,projection.surfaces[k].length]));return {label,expected,context:lp24449b1.identity(),published,fresh:fresh.activeAwareness.activeAwarenessCount,counts,location:document.getElementById('mobileAwarenessPanelIssues').innerText,pulseDOM:document.getElementById('gridlyCommunityPulseSubline').innerText,markers:unifiedIncidentLayer.getLayers().map(m=>m.options.incidentId),activeRailMarkers:[...crossingMarkers].filter(([,m])=>m.options.gridlyCommunityStatus==='active').map(([id])=>id),coverage:gridlyGetLocalSourceCoverage(),pass:published===expected&&fresh.activeAwareness.activeAwarenessCount===expected&&Object.values(counts).every(n=>n===expected)&&!document.body.innerText.includes('undefinedm')};}
 };});
 const mixed=[['road-only',['flooded-roadway'],'HOME'],['two-road',['flooded-roadway','debris-in-road'],'HOME'],['road-delay',['flooded-roadway','reported-crossing-delay'],'HOME'],['two-crossings',['train-blocking-crossing','reported-crossing-delay'],'HOME'],['road-provider-unavailable',['flooded-roadway'],'HOME'],['Search',['flooded-roadway'],'SEARCH'],['AroundMe',['flooded-roadway'],'AROUND_ME'],['Home',['flooded-roadway'],'HOME'],['three-hazard-sequential',['flooded-roadway','debris-in-road','downed-power-line'],'HOME']];
 for(const [name,types,context]of mixed){const result=await p.evaluate(async({name,types,context})=>{await b1Clear.reset();if(context==='SEARCH')await b1Clear.search('Dayton');if(context==='AROUND_ME')await b1Clear.around();const fixtures=await b1Clear.add(types);await b1Clear.publish();const states=[b1Clear.capture('active',types.length)];for(let i=0;i<fixtures.length;i++){gridlyLocalTestReports.clearOne(fixtures[i].id);await b1Clear.settle();states.push(b1Clear.capture(`clear-${i+1}`,types.length-i-1));await b1Clear.publish();states.push(b1Clear.capture(`refresh-${i+1}`,types.length-i-1));}return {name,types,context,fixtures,states,pass:states.every(r=>r.pass)};},{name,types,context});records.push(result);write(`${out}/clear-lifecycle.json`,{records,network:evidence});console.log(JSON.stringify({name,pass:result.pass,counts:result.states.map(s=>[s.expected,s.published,s.fresh])}));}
 for(const mode of ['clear-search','search-clear','around-clear','return-home-clear','county-clear','area-clear']){const result=await p.evaluate(async mode=>{await b1Clear.reset();if(mode==='return-home-clear')await b1Clear.search('Liberty');const home=lp24449b1.bytes(),[fixture]=await b1Clear.add(['flooded-roadway']);await b1Clear.publish();const states=[b1Clear.capture('active',1)];if(mode==='clear-search')gridlyLocalTestReports.clearOne(fixture.id);if(['clear-search','search-clear'].includes(mode))await b1Clear.search('Cleveland');if(mode==='around-clear')await b1Clear.around();if(mode==='return-home-clear')await b1Clear.home();if(['county-clear','area-clear'].includes(mode)){document.querySelector(`#gridlyPortraitV2 [data-geo-filter="${mode==='county-clear'?'county':'town'}"]`).click();await b1Clear.settle();}if(mode!=='clear-search')gridlyLocalTestReports.clearOne(fixture.id);await b1Clear.settle();states.push(b1Clear.capture('cleared-current-context',0));await b1Clear.publish();states.push(b1Clear.capture('refreshed',0));await b1Clear.home();states.push(b1Clear.capture('returned',0));return {name:mode,states,homePreserved:JSON.stringify(home)===JSON.stringify(lp24449b1.bytes()),pass:states.every(r=>r.pass)&&JSON.stringify(home)===JSON.stringify(lp24449b1.bytes())};},mode);records.push(result);write(`${out}/clear-lifecycle.json`,{records,network:evidence});console.log(JSON.stringify({name:mode,pass:result.pass}));}
 for(const order of ['provider-before-clear','provider-queued-at-clear','provider-after-clear']){const result=await p.evaluate(async order=>{await b1Clear.reset();const [fixture]=await b1Clear.add(['flooded-roadway']);if(order==='provider-before-clear')await b1Clear.publish();const before=b1Clear.capture('active',1);const pending=loadSharedReports('interval_live_refresh');if(order==='provider-queued-at-clear')gridlyOfficialProviderConsumerRefresh({providerId:'drivetexas',reason:'interval_live_refresh'});gridlyLocalTestReports.clearOne(fixture.id);if(order==='provider-after-clear')gridlyOfficialProviderConsumerRefresh({providerId:'drivetexas',reason:'interval_live_refresh'});refreshGridlyCommunityPulseSharedModel({reason:'b1-concurrent-clear'});gridlyGetGovernedConsumerProjection();await Promise.resolve(pending);await b1Clear.settle();const states=[before,b1Clear.capture('after-overlap',0)];await gridlyRunAlertsBackgroundRefreshAfterOpen('alerts_open_background_refresh');await b1Clear.settle();states.push(b1Clear.capture('after-alerts-refresh',0));return {name:order,states,pass:states.every(r=>r.pass)};},order);records.push(result);write(`${out}/clear-lifecycle.json`,{records,network:evidence});console.log(JSON.stringify({name:order,pass:result.pass}));}
 write(`${out}/clear-lifecycle.json`,{records,network:evidence,passed:records.every(r=>r.pass)});
}finally{await session.close();}
